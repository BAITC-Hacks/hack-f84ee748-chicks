# building

Строительство. catalog.js — типы и размеры проектов; index.js — предпросмотр и подтверждение; style.css — оформление. Получает ctx, возвращает render(). Решение проводится через City.place и ctx.afterDecision.

Общая инструкция: корневые README.md и КОМАНДА.md.

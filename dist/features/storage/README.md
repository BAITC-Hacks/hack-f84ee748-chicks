# storage

Сохранения. index.js читает и записывает localStorage. load валидирует данные через City.validate. Новый ключ v3 сохраняет старые данные отдельно. Ошибки доступа к хранилищу не ломают игру.

Общая инструкция: корневые README.md и КОМАНДА.md.
